
table.table
  thead
    tr
      th Название проекта
      th Сферы деятельности
      th Авторы
  tbody
    - @projects.each do |project|
      tr
        td = project.link
        td = project.scope_names
        td = project.author_names

= paginate @projects
