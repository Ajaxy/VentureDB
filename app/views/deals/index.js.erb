var $middle = $('#container').find('.middle');

$middle.append("<%=j render partial: 'deal_row', collection: @deals %>");
$(".more").remove();

<% unless @deals.last_page? %>
    $middle.append("<%=j render "more-link", collection: @deals %>");
<% end %>
