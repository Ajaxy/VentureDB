doctype html
html
  head
    title VentureDB

    = render "google-fonts"
    = render "google-analytics"

    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = favicon_link_tag       "favicon.png"
    = csrf_meta_tags

    = yield :head

    /[ if lt IE 9 ]
      = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"

  body
    #wrapper
      #middle
        #container
          #content
            header
              #logo
                p Venture Database
              #search_box
                = form_tag "/search", method: :get, class: "search" do
                  = text_field_tag :search, params[:search], placeholder: 'Например, TechCrunch Moscow 2012'
                  button type="submit" = "ПОИСК"
            .main-content = yield
            - unless @hide_footer
              footer.clearfix
                h2 О проекте
                .col
                  .link = link_to "Инвесторам", "/about/#investors"
                  .link = link_to "Компаниям", "/about/#companies"
                  .link = link_to "Корпорациям", "/about/#corporations"
                  .link = link_to "Инфраструктуре", "/about/#infrastructure"
                .col
                  .link = link_to "Для государства", "/about/#state"
                  .link = link_to "Методика работы", "/about/#methodics"
                  .link = link_to "Команда", "/about/#team"
                  .link = link_to "Обратная связь", :feedback
        aside#menu
          = render partial: 'menu'
          nav
            ul
              li = link_to "О проекте", :about
              - if current_user.admin?
                li = link_to "Панель управления", "/admin"
              li = link_to current_user.email, "#"
              li = link_to "Выйти", :destroy_user_session, method: "delete"
