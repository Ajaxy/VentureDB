.pull-right = link_to "Новая компания", [:new, :admin, :company], class: "btn btn-primary"

table.table
  thead
    tr
      th = @sorter.header "Название", :name
      th Тип компании
      th Местонахождение
  tbody
    - @companies.each do |company|
      tr
        td = company.link(scope: :admin)
        td = Company::TYPES[company.type_id]
        td = company.place

= paginate @companies
